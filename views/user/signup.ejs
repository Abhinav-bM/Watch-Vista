<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <title>WatchVista - Signup</title>
    <link
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        display: flex;
        align-items: center;
        justify-content: center;
        height: 100vh;
        background-color: #fff;
      }

      .signup-container {
        max-width: 500px;
        width: 100%;
        padding: 1px;
        text-align: center;
      }

      .logo {
        max-width: 100px;
        margin-bottom: 20px;
      }

      .form-control {
        border: 2px solid #ced4da;
        border-radius: 5px;
        height: 46px;
      }

      .btn-primary {
        background-color: #000;
        border-color: #000;
        height: 46px;
        color: #fff;
        /* Set text color to white */
      }

      .btn-primary:hover {
        background-color: #000;
        /* No hover effect */
        border-color: #000;
      }

      /* Remove underline from <a> tag */
      a {
        text-decoration: none;
      }

      a {
        text-decoration: none;
        color: #000;
        /* Set link color to black */
      }

      h2 {
        font-weight: bolder;
        font-size: 28px;
      }

      */

    /* Additional style for terms and conditions checkbox */
    .form-check-label {
        font-size: 14px;
      }
    </style>
  </head>

  <body>
    <div class="signup-container">
      
      <img src="/img/logo/logo1.png" alt="" style="max-width: 180px;" />
      <!-- <h2 class="text-center mb-4">Signup</h2> -->

      <form id="signupForm" method="post" class=" pt-4 px-3">
     
        <label for="userName">Name <span>**</span></label>
        <small id="name-error" style="color: red; display: none"
          >Invalid Name</small
        >
        <input
          id="userName"
          class="form-control my-0 py-2"
          type="text"
          name="userName"
          placeholder="Enter your name"
        />

        <label for="email-id" class="py-2">Email Address <span>**</span></label>
        <small id="email-error" style="color: red; display: none"
          >Invalid email</small
        >
        <input
          id="email-id"
          class="form-control my-0 py-2"
          type="text"
          name="email"
          placeholder="Enter Email address..."
        />

        <label for="phone" class="py-2">Phone <span>**</span></label>
        <small id="phone-error" style="color: red; display: none"
          >Invalid phone</small
        >
        <input
          id="phone"
          class="form-control my-0 py-2"
          type="text"
          name="phoneNumber"
          placeholder="Enter phone number"
        />

        <label for="pass" class="py-2">Password <span>**</span></label>
        <small id="password-error" style="color: red; display: none"
          >Invalid password</small
        >
        <input
          id="pass"
          class="form-control mb-4 py-2"
          type="password"
          name="password"
          placeholder="Enter password..."
        />

        <div class="mt-10"></div>
        <button
          class="btn btn-primary btn-block"
          type="button"
          onclick="validateAndSendOTP()"
        >
          Register now
        </button>
        <div class="or-divide py-1"><strong>or</strong></div>
      </form>
      <div class=" px-3">
        <button class="btn btn-primary btn-block">
          <a href="/login" class="text-decoration-none text-light">Login Now</a>
        </button>
      </div>
      

      <!-- Bootstrap Modal for OTP Verification -->
      <div
        class="modal fade"
        id="otpModal"
        tabindex="-1"
        role="dialog"
        aria-labelledby="otpModalLabel"
        aria-hidden="true"
      >
        <div class="modal-dialog modal-dialog-centered" role="document">
          <div class="modal-content">
            <div class="modal-header">
              <h5 class="modal-title" id="otpModalLabel">Enter OTPs</h5>
              <button
                type="button"
                class="close"
                data-dismiss="modal"
                aria-label="Close"
              >
                <span aria-hidden="true">&times;</span>
              </button>
            </div>
            <div class="modal-body">
              <div class="form-group">
                <label for="phoneOtpInput">Phone OTP</label>
                <input
                  type="text"
                  class="form-control"
                  id="phoneOtpInput"
                  placeholder="Enter Phone OTP"
                />
              </div>
              <div class="form-group">
                <label for="emailOtpInput">Email OTP</label>
                <input
                  type="text"
                  class="form-control"
                  id="emailOtpInput"
                  placeholder="Enter Email OTP"
                />
              </div>
              <small id="otp-error" class="text-danger"></small>
            </div>
            <div class="modal-footer">
              <button
                type="button"
                class="btn btn-secondary"
                data-dismiss="modal"
              >
                Close
              </button>
              <button type="button" id="verifyOtpBtn" class="btn btn-primary">
                Verify OTPs
              </button>
            </div>
          </div>
        </div>
      </div>
    </div>
    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <!-- Bootstrap JS -->
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script>
      // VERIFY OTP
      $(document).on("click", "#verifyOtpBtn", function () {
        console.log("Verify OTP button clicked"); // Check if function is being called

        var nameInput = document.getElementById("userName").value;
        var emailInput = document.getElementById("email-id").value;
        var phoneInput = document.getElementById("phone").value;
        var passwordInput = document.getElementById("pass").value;

        var phoneOtpInput = document.getElementById("phoneOtpInput").value;
        var emailOtpInput = document.getElementById("emailOtpInput").value;
        var otpError = document.getElementById("otp-error");

        // Check if OTP fields are not empty
        if (phoneOtpInput.trim() === "" || emailOtpInput.trim() === "") {
          otpError.textContent = "Please enter both OTPs";
          otpError.style.display = "block";
          return;
        }

        // Send OTPs to backend for verification
        const formData = {
          userName: nameInput,
          email: emailInput,
          phoneNumber: phoneInput,
          password: passwordInput,

          phoneOtp: phoneOtpInput,
          emailOtp: emailOtpInput,
        };

        fetch("/verify-otp", {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
          },
          body: JSON.stringify(formData),
        })
          .then((response) => {
            if (response.ok) {
              return response.json();
            } else {
              throw new Error("Failed to verify OTPs");
            }
          })
          .then((result) => {
            if (result.success) {
              $("#otpModal").modal("hide");
              // Redirect user to dashboard or any other page
              window.location.href = "/";
            } else {
              // OTPs are invalid
              otpError.textContent = "Invalid OTPs. Please try again.";
              // otpError.style.display = 'block';
            }
          })
          .catch((error) => {
            console.error("Error verifying OTPs:", error);
            otpError.textContent = "Invalid OTPs. Please try again.";
            // otpError.style.display = 'block';
          });
      });

      function validateAndSendOTP() {
        var nameInput = document.getElementById("userName").value;
        var emailInput = document.getElementById("email-id").value;
        var phoneInput = document.getElementById("phone").value;
        var passwordInput = document.getElementById("pass").value;

        var nameError = document.getElementById("name-error");
        var emailError = document.getElementById("email-error");
        var phoneError = document.getElementById("phone-error");
        var passwordError = document.getElementById("password-error");

        var isValidName = /^[a-zA-Z\s]{3,}$/.test(nameInput);
        var isvalidEmail = /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(emailInput);
        var isvalidPhone = /^(\+91)?\d{10}$/.test(phoneInput);
        var isvalidPassword = passwordInput.length >= 6;

        // Show/hide error messages based on validation
        nameError.style.display = isValidName ? "none" : "block";
        emailError.style.display = isvalidEmail ? "none" : "block";
        phoneError.style.display = isvalidPhone ? "none" : "block";
        passwordError.style.display = isvalidPassword ? "none" : "block";

        // Return if any validation fails
        if (
          !isValidName ||
          !isvalidEmail ||
          !isvalidPhone ||
          !isvalidPassword
        ) {
          console.log("Form validation failed.");
          return;
        }

        // IF VALIDATION IS OK THEN MOVE TO SEND OTP
        const formData = {
          userName: nameInput,
          email: emailInput,
          phoneNumber: phoneInput,
          password: passwordInput,
        };

        // Send data to backend to start OTP verification process
        fetch("/user/send-otp", {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
          },
          body: JSON.stringify(formData),
        })
          .then((response) => {
            if (response.ok) {
              // Show the Bootstrap modal
              $("#otpModal").modal("show");
            } else {
              console.log("Failed to send OTP");
            }
          })
          .catch((error) => {
            console.error("Error sending OTP:", error);
          });
      }
    </script>
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
  </body>
</html>
